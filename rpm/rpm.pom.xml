<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	       xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <packaging>rpm</packaging>
  <modelVersion>4.0.0</modelVersion>
  <artifactId>pth_07</artifactId>
  <version>${revision}${sha1}${changelist}</version>
  <name>pth_07</name>
  <description>pth_07</description>
  <groupId>com.teragrep</groupId>
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>1.8</maven.compiler.source>
    <maven.compiler.target>1.8</maven.compiler.target>
    <java.version>1.8</java.version>
    <revision>0.0.1</revision>
    <changelist>-SNAPSHOT</changelist>
    <sha1/>
  </properties>
  <build>
    <directory>${project.basedir}/target</directory>
    <plugins>
      <plugin>
	      <groupId>org.apache.maven.plugins</groupId>
	      <artifactId>maven-surefire-plugin</artifactId>
	      <configuration>
          <skipTests>true</skipTests>
	      </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>rpm-maven-plugin</artifactId>
        <version>2.2.0</version>
        <extensions>true</extensions>
        <executions>
          <execution>
            <id>default-rpm</id>
            <goals>
              <goal>rpm</goal>
            </goals>
            <phase>package</phase>
          </execution>
        </executions>
        <configuration>
          <summary>${project.groupId}-${project.artifactId}</summary>
          <name>${project.groupId}-${project.artifactId}</name>
          <version>${project.version}</version>
          <release>${env.BUILD_ID}</release>
          <license>Proprietary</license>
          <distribution>teragrep Log Management Suite</distribution>
          <vendor>https://teragrep.com/</vendor>
          <packager>teragrep &lt;servicedesk@teragrep.com&gt;</packager>
          <group>teragrep/LogManagementSuite</group>
          <needarch>false</needarch>
          <defaultUsername>srv-zpln</defaultUsername>
          <defaultGroupname>srv-zpln</defaultGroupname>
          <defaultFilemode>0644</defaultFilemode>
          <defaultDirmode>0755</defaultDirmode>
          <!-- disable bytecompile as python3 scripts compiled by python2 often causes fatal errors -->
          <defineStatements>
            <!-- _python_bytecompile_extra 0 doesn't work so we take the original script and just replace what we do not want -->
            <defineStatement>__os_install_post %(echo '%{__os_install_post}' | sed -e 's!/usr/lib[^[:space:]]*/brp-python-bytecompile[[:space:]].*$!!g')</defineStatement>
            <defineStatement>_build_id_links none</defineStatement>
            <defineStatement>__provides_exclude ^osgi\\(.*$</defineStatement>
            <defineStatement>__requires_exclude ^osgi\\(.*$</defineStatement>
          </defineStatements>
          <mappings>
            <mapping>
              <directory>/opt/teragrep/${project.artifactId}/lib</directory>
              <directoryIncluded>true</directoryIncluded>
              <filemode>755</filemode>
              <dirmode>755</dirmode>
              <username>srv-zpln</username>
              <groupname>srv-zpln</groupname>
              <recurseDirectories>true</recurseDirectories>
              <sources>
                <source>
                  <location>${project.basedir}/../target/pth_07-${revision}${changelist}${sha1}.jar</location>
                  <destination>pth_07.jar</destination>
                </source>
              </sources>
            </mapping>
            <mapping>
              <directory>/opt/teragrep/${project.artifactId}/bin</directory>
              <directoryIncluded>true</directoryIncluded>
              <sources>
                <source>
                  <location>${project.basedir}/../src/main/resources/inject_config.py</location>
                </source>
              </sources>
            </mapping>
            <mapping>
              <directory>/opt/teragrep/${project.artifactId}/share</directory>
              <directoryIncluded>true</directoryIncluded>
              <sources>
                <source>
                  <location>${project.basedir}/../src/main/resources/inject_config.json</location>
                </source>
              </sources>
            </mapping>
          </mappings>
          <requires>
            <require>python3</require>
            <require>com.teragrep-zep_01</require>
          </requires>
          <postinstallScriptlet>
            <script>
python3 /opt/teragrep/${project.artifactId}/bin/inject_config.py
            </script>
          </postinstallScriptlet>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-deploy-plugin</artifactId>
        <version>2.8.2</version>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <executions>
          <execution>
            <id>default-compile</id>
            <phase>compile</phase>
            <goals>
              <goal>compile</goal>
            </goals>
            <configuration>
              <skipMain>true</skipMain>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
